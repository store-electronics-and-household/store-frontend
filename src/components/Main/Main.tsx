import React from 'react';
import './Main.css';
import Slider from '../Slider/Slider';

const Main: React.FC = () => {
  return (
    <section className='main'>
      <Slider />
    </section>
  );
};

export default Main;
